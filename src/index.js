const React = require("react");
const ReactDOM = require("react-dom");

ReactDOM.render(
  <div>
    <h1>Tonight's ToDos!</h1>
    <ul>
      <li>The Same Thing</li>
      <li>We do every night</li>
      <li>Try to take over the world</li>
    </ul>
  </div>,
  document.getElementById("root")
);
